type Query {
    hello: String
    posts: [Post]
}

type Post {
   id: String!
   title: String
   body: String
   comments: [Comment]
}

type Comment {
   id: String!
   name: String
   email: String
   body: String
}